[package]
name = "boost_throw_exception"
description = "Boost C++ library boost_throw_exception packaged using Zanbil"
version = "0.1.0"
edition = "2024"
links = "boost_throw_exception"
license = "BSL-1.0"
repository = "https://github.com/zanbilify/boost_throw_exception"

[package.metadata.zanbil]
make_dependencies_public = true

[dependencies]
boost_assert = { version = "0.1.0", path = "../boost_assert" }
boost_config = { version = "0.1.0", path = "../boost_config" }

[build-dependencies]
zanbil-boost-build = "0.1.0"
